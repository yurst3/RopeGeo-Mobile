import type { FontProfile } from "../types";

/** Platform system default (San Francisco / Roboto); omit custom `fontFamily`. */
export const autoFontProfile: FontProfile = {
  display: {},
  body: {},
  ui: {},
};
