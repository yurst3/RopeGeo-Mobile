/**
 * Horizontal inset for filter sheet content. Sheet body has no horizontal padding so
 * full-bleed rows (e.g. disabled switch band) can use negative margin to reach panel edges.
 * Header, scroll content, and footer apply this inset explicitly.
 */
export const FILTER_SHEET_HORIZONTAL_INSET = 16;
